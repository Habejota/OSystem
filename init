#io.flush("lib\main.lib") 
adrr, calibration = ads.init()
adrr.getfenv("" ..tostring(byte(1024)))
while calibration:
	physics.start()
	physics.setScale(calibration)
	
	assert physics, calibration
system.activate(adrr)
table.concat(io.lines("kernel.py"))